# 已验证清单（附真实执行证据）

> 每条都以**实际执行输出**为依据，不是设计意图。证据来源：终端日志、浏览器网络记录、程序自测。
> 工作目录：`<工程根>\`
> 尚未验证的部分见本文第 8 节。

## 1. 平台与播放器

| 项目 | 结果 | 证据 |
|---|---|---|
| 本地站点可访问 | ✅ | `curl http://127.0.0.1:8790/player/index.html` → **200**；`/player/stories/index.json` → 200 |
| 完整游玩流程 | ✅ | 浏览器实机：7 段、6 次选择 → 结局页含「关键选择回顾」逐段列出、解锁进度 1/3、重玩/返回/分享按钮 |
| A 档全部 8 篇可玩通 | ✅ | 逐篇自动化：sc-0001(7段) sc-0002(5) sc-0003(5) sc-0004(5) sc-0005(6) sc-0006(7) sc-0007(5) sc-0008(6)，**8/8 均到达结局** |
| 网络隔离（核心要求） | ✅ | 整局游玩只发出 **1 个** 网络请求（`./stories/sc-0006.json` 预制故事文件）；**0 次模型调用、0 个生成端点** |
| 无外部依赖 | ✅ | 页面资源清单只有本地 `127.0.0.1:8790` 的 css/js/svg/json（`performance.getEntriesByType('resource')`） |
| 刷新后恢复进度 | ✅ | 清空 localStorage/sessionStorage → 玩到第 4 段 → 刷新 → **仍回到第 4 段** |
| 分享深链 | ✅ | 静态哈希深链 `#/s/<storyId>` 打开同一份已保存故事，URL 中无聊天/编辑内容 |
| 减少动态效果 | ✅ | 首页与故事页均有开关 |
| 截图 | ✅ | 桌面 1280×900 与手机 375×812：首页、故事页、游玩中、结局页；存 `screenshots/`（**7 张唯一图**，桌面 4 + 手机 3） |
| 凭据扫描 | ✅ | `node tools/pack.cjs` → 密钥样式扫描通过，发布包内无 `.env`、无生成工具 |

## 2. 结构验证器与程序检查（自测）

| 测试文件 | 结果 |
|---|---|
| `tests/local-core.cjs` | **通过 34 / 失败 0** |
| `tests/merges-core.cjs` | **通过 9 / 失败 0** |
| `tests/props-core.cjs` | **通过 8 / 失败 0** |
| `tests/outline-core.cjs` | 通过 19 |
| `tests/facts-core.cjs` | 通过 |

已验证的关键不变式：起点存在、节点 ID 唯一、全节点可达、全结局可达、无死路、
初版无环、路径 ≤128、选项不重复、伪分支检测、正文长度范围。
图形模板不变式：`shapes.verifyAll()` 五张图全过（节点数与路径数逐层核对）。

## 3. 模型调用层

| 项目 | 结果 | 证据 |
|---|---|---|
| 服务返回 token 用量 | ✅ | 日志中 token 均来自服务端 `usage` 字段（**非**字数估算）；未返回用量的调用数 = 0 |
| 吞吐实测 | ✅ | 并发 1：**12/12 成功**；并发 2：2/12；并发 4：5/12 → 因此默认并发降为 **1** |
| 退避与重试 | ✅ | 429/网络故障按 `[1500,3500,7000,12000,20000]` 有界退避；连续失败达阈值自动暂停批次 |
| JSON 截断修复 | ✅ | 自测 **8/8** 通过（代码块容错、尾随逗号、未闭合括号）；纯垃圾输入仍被拒绝（**不编造**） |
| 预算硬上限与记账口径 | ✅ | 每一次 HTTP 尝试都计入（含重试），日志行数与 `budget.json` 一致；达到上限批次自动暂停并保留恢复位置 |
| 审核调用预算 | ✅ | 审核类调用 maxTokens 提到 16000（该模型把推理计入输出预算，6000 会被推理吃光导致空返回） |

## 4. 内容流水线的收敛性（本轮最关键验证）

| 项目 | 结果 | 证据 |
|---|---|---|
| 定点回归替代全篇重审 | ✅ | sc-0005：曾隔离（遗留 3 条）→ 改为定点回归后 **1 轮修复即通过**；sc-0001：曾隔离 → **1 轮修复通过** |
| 证据定位（无 nodeId 问题） | ✅ | 用隔离记录回归测试：原本无法定位的问题**均成功定位**（`locatedBy: evidence` / `choice-text`） |
| 2 轮修复硬上限 | ✅ | 代码强制 `fixRounds >= 2` 停止；隔离记录中保留完整正文与审核结论 |
| 验收门（证据核验） | ✅ | 无法从原文核验的 blocker/major 机械降级为 minor（`demoted=true`），自测 4 例正确 |
| sc-0006 收敛 | ✅ | **编辑审核 pass（0 issues）、路径审核 pass（0 issues）** → `run-sc-0006review37.txt`（v33 定稿：补容器转换与动机转折、兑现承认动作）；验收门通过写入正式库，**库与草稿完全同步** |
| 一致性专项工具（新增） | ✅ | `tools/content/consistency-review.cjs`：输入极小（只送 label/选项与其落点正文），专查「label↔正文」「选项↔落点」。**判别力已用注入缺陷回归验证：3/3 命中**（`run-sc-0006consistencyTest.txt`）；完好版本 25 条目 0 不一致（`run-sc-0006consistency3.txt`） |
| 隔离库清空 | ✅ | `content/stories/` 8 篇，`content/quarantine/` 无待处置作品（仅 `archive/` 历史记录） |

## 5. 已通过的精品故事（A 档 8/8）

| ID | 题材 | 标题 | 节点 | 结局 | 修复轮数 | 遗留问题 |
|---|---|---|---|---|---|---|
| sc-0001 | 温暖日常 | 凌晨的货架底层 | 16 | 3 | 1 | 0 |
| sc-0002 | 职场喜剧 | 第七遍闹铃没人认领 | 16 | 3 | 2 | 0 |
| sc-0003 | 轻悬疑 | 这栋楼不叫那个名字 | 14 | 3 | 人工定稿 | 0 |
| sc-0004 | 旅行事故 | 全价票与三日游 | 12 | 3 | 0 | 0 |
| sc-0005 | 宠物奇遇 | 猫在搬家那天从纸箱里消失了 | 14 | 3 | 1 | 0 |
| sc-0006 | 生日惊喜 | 天亮前的咸蛋糕 | 14 | 3 | 人工定稿 | 0 |
| sc-0007 | 轻科幻 | 该用户已归档 | 14 | 3 | 1 | 0 |
| sc-0008 | 宿舍日常 | 冰箱滴水，无人认领 | 14 | 3 | 1 | 0 |

人工抽查：sc-0001 三个结局**实质不同**（店长顶下 / 偷偷给他 / 店长自己柜子里也有同款）；
sc-0006 三个结局分属「圆满 / 苦甜 / 转折」且由不同关键选择导向 —— 满足任务书对结局区别的要求。

## 6. 评测集

| 项目 | 结果 | 证据 |
|---|---|---|
| 评测输入 | ✅ **2400 条**（目标 2000–10000） | `content/eval/evalset.jsonl` |
| 题材覆盖 | ✅ 8 题材 × 300 条 | `content/eval/evalset-summary.json` |
| 机制覆盖 | ✅ pure-dag / trust-int / enum-route / bool-flag / merge-back / fake-branch-trap / long-path / three-enders，各 296–304 条 | 同上 |
| 难度分布 | ✅ 简单 783 / 常规 826 / 偏难 791 | 同上 |
| 生成方式 | ✅ **纯程序生成，零模型调用**（可复现 seed） | `node tools/content/evalset.cjs --n 2400 --seed 7` |

## 7. 预算与吞吐（本地累计记录）

| 指标 | 值 |
|---|---|
| 请求尝试总数 | **1915**（上限 2000；成功 1013、失败 217、其中重试 685） |
| token（服务端返回） | 输入 3,315,849 + 输出 4,303,504 = **7,619,353** |
| 余额 | **不可查询** → 以本地累计记录设限，**误差未知**（已标注） |
| 与「梗一下」的额度隔离 | 本工程独立上限 2000 次请求，为第一任务保留容量 |

## 8. 尚未完成（如实列出）

| 项目 | 状态 | 说明 |
|---|---|---|
| B 档 80 篇 | **未生产** | 规划已就绪（`plan-b.cjs`，ID `sc-0009…sc-0080`）；A 档把开发期预算用在打通收敛率上，剩余 222 次请求不足以支撑 80 篇批量 |
| C 档 300–1000 篇 | **未生产** | 同上；任务书明确「质量、吞吐或剩余时间不支持扩展时，停在可验收档位，交付实际成果」 |
| B/C 档单件真实成本 | 未实测 | 报告中的单件成本是开发期**上界**（222.3 请求/篇，含反复调试），需 B 档首批 20 篇才能得出真实值 |
| 带状态（条件/枚举）故事 | 未生产 | schema 与校验器已支持带状态可达性检查，但 A 档 8 篇均为无状态 DAG |
| 128 条路径的复杂图 | 未使用 | 实测 8 层/128 路径反复不收敛，已移入 `SHAPES_BACKLOG`；默认池为 6 层/32 路径与 7 层/64 路径 |
