# 浏览器验证记录

> 全部为**真实浏览器**（Chromium via CDP）实际操作，非代码审查或模拟。
> 本地站点：`http://127.0.0.1:8790/player/index.html`（`node server/serve.cjs`）

## 一、资源可用性

```
GET /player/index.html            200
GET /player/stories/index.json    200
GET /player/stories/sc-0006.json  200
GET /player/js/app.js             200
GET /player/css/app.css           200
```

## 二、完整游玩（sc-0006《天亮前的咸蛋糕》）

**操作**：首页 → 深链 `#/s/sc-0006` → 从头开始 → 连续 6 次选择 → 结局页

**轨迹**（每步均有点击与页面文本确认）：

| 步 | 段 | 选择 |
|---|---|---|
| 1 | 天亮前的厨房 | 再想办法补救一下 |
| 2 | 橱柜里只剩空糖袋 | 敲门问小满有没有糖 |
| 3 | 小满尝了一口咸蛋糕 | 硬着头皮把盘子端上桌 |
| 4 | 把盘子端上桌 | 把话憋住，只把盘子放下 |
| 5 | 蜡烛点上之后 | 把咸味说成海盐特调 |
| 6 | 阿栗尝了第一口 | 让她直说，好不好吃 |
| 7 | （结局）咸的也挺好吃 | — |

**结局页内容确认**：结局名「咸的也挺好吃」、正文、`走了 7 段 / 做了 6 个选择 / 已解锁 1/3 个结局`、
「关键选择回顾」逐段列出 6 次选择（含节点标题）、三个结局的解锁状态（1 已解锁 + 2 未解锁）、
`再玩一次` / `返回故事库` / `分享这个故事` 三个按钮。

## 三、网络隔离验证（核心要求：正式产品运行完全不依赖模型 API）

**方法**：页面加载后注入 `fetch` / `XMLHttpRequest` hook，记录整局游玩发出的**全部**请求。

**结果**：

```
[".../stories/sc-0006.json"]
```

**整局游玩只有 1 个请求**，指向预制故事 JSON；**0 次模型调用、0 个生成端点、0 个外链**。

**交叉验证**（`performance.getEntriesByType('resource')`）：本页加载的全部资源均为 `127.0.0.1:8790` 下的
本地文件 —— `css/app.css`、`lib/schema.js`、`lib/runtime.js`、`js/storage.js`、`js/share.js`、`js/player.js`、
`js/app.js`、`stories/index.json`、`icon.svg`、各故事 JSON。**无任何外部域名**。

## 四、A 档全部 8 篇可玩通

| 故事 | 段数（到结局） | 到达结局 | 请求数 |
|---|---|---|---|
| sc-0001 | 7 | ✅ | 仅故事 JSON |
| sc-0002 | 5 | ✅ | 仅故事 JSON |
| sc-0003 | 5 | ✅ | 仅故事 JSON |
| sc-0004 | 5 | ✅ | 仅故事 JSON |
| sc-0005 | 6 | ✅ | 仅故事 JSON |
| sc-0006 | 7 | ✅ | 仅故事 JSON |
| sc-0007 | 5 | ✅ | 仅故事 JSON |
| sc-0008 | 6 | ✅ | 仅故事 JSON |

## 五、刷新恢复

清空浏览器存储 → 进入故事 → 做 3 次选择（到第 4 段）→ **刷新页面** → 仍回到第 4 段，选择历史完整保留。

## 六、分享链接

分享按钮生成静态哈希深链（`#/s/<storyId>`）。直接打开该链接 → 加载**同一份已保存故事**，
无生成等待、无需模型调用；URL 中不含任何聊天内容或编辑信息。

## 七、视口与截图

| 视口 | 页面 | 文件 |
|---|---|---|
| 1280×900 | 首页 | `screenshots/首页-桌面-1280x900.png` |
| 1280×900 | 故事页 | `screenshots/故事页-桌面-1280x900.png` |
| 1280×900 | 游玩中（第 4 段） | `screenshots/游玩中-桌面-1280x900.png` |
| 1280×900 | 结局页（圆满·咸的也挺好吃） | `screenshots/结局页-桌面-1280x900.png` |
| 375×812 | 首页 | `screenshots/首页-手机-375x812.png` |
| 375×812 | 游玩中（第 3 段） | `screenshots/游玩中-手机-375x812.png` |
| 375×812 | 结局页（转折·其实阿栗早就知道） | `screenshots/结局页-手机-375x812.png` |

共 **7 张**，内容各不相同（桌面与手机各覆盖首页/游玩中/结局页，桌面另含故事页）。
移动端布局确认：卡片单列、选项按钮宽度自适应、正文行长适中、无横向滚动。

## 八、离线包（独立运行已验证）

`outputs\如果当时-离线包.zip`（57 KB）：

| 检查项 | 结果 |
|---|---|
| 是真 zip（非改名 tar） | ✅ 文件头 `PK`；`file` 识别为 Zip archive |
| 跨平台可解压 | ✅ 路径用正斜杠、文件名 UTF-8，`unzip` 正常解压 |
| CRC 完整性 | ✅ 22 个文件全部 OK，`testzip()` 无损坏 |
| 解压到**干净目录**后独立运行 | ✅ `node offline-server.cjs` → 端口 8791 |
| 离线包内完整游玩 | ✅ sc-0006 走到结局（7 段），故事卡 8 张齐全 |
| 离线包网络审计 | ✅ 整局只有 1 个请求（`./stories/sc-0006.json`），资源全部来自 `127.0.0.1:8791` |

全部内容随包提供（无外链字体/图片/CDN）。在线站点若启用离线缓存，也只有已取回的内容可断网游玩，
不假装首次访问即可离线。
