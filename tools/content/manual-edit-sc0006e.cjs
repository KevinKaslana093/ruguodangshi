// 人工式定点编辑（终稿 v3）：sc-0006 收尾三条。
// 病根：结局断言了「这盘是咸的」，但部分来路端上的是加糖重烤的第二炉（甜的）。
// 结局节点是**多路汇合**，不能断言任一分支才成立的口味。
// 同时 n9 必须恢复「小满开口打圆场」，因为它的选项写的是"顺着小满的圆场编下去"。
const fs = require('fs');
const path = require('path');
const R = path.resolve(__dirname, '..', '..');
const p = path.join(R, 'content', 'drafts', 'sc-0006.json');
const d = JSON.parse(fs.readFileSync(p, 'utf8'));
const s = d.story;
const byId = {};
s.nodes.forEach(n => { byId[n.id] = n; });

/* ① n9：恢复小满打圆场的动作（选项承诺要求），但不指定"新试做法"这种与来路冲突的说法 */
byId['n9'].text = '窗外刚泛出一点灰，阿栗坐在桌边，蜡烛点着，火苗把影子压在墙上。蛋糕摆在桌子中间，阿栗拿起刀，手腕刚抬起来。小满先开了口，笑着说这盘得先尝一口再评，手艺的事待会儿再说。阿栗的刀停在半空，先看小满，又看小陶。小陶站在旁边，嘴张开一半，话还没出来。';

/* ② n12 结局（e1）：不断言口味（来路可能是咸的原炉，也可能是加糖的第二炉） */
byId['n12'].text = '阿栗又叉了一块，含在嘴里，慢慢点了点头，说这样也挺好吃，像哪家铺子会做的点心。小满先没忍住，笑出声，伸手也切了一角。小陶的脸烫起来，叉子犹豫着跟过去。三个人围着那盘颜色不匀的蛋糕，一人一角地挖，蜡烛烧到一半，火苗在没开灯的屋里晃。天从灰里透出一点白，盘底慢慢刮出了声音。';

/* ③ n13 结局（e2）：同样不确定口味，只写"这盘东西" */
byId['n13'].text = '蜡烛烧到一半，蜡油在盘边积了一小滩。阿栗把手里那一角吃完，小陶和小满也各挖了一块，吃着吃着，动作都慢下来。盘子里没人再动，切面敞着。谁也没再提这盘东西是怎么来的。窗外那片灰终于泛出白，屋里的灯始终没开。';

/* ④ n14 结局（e3）：去掉对具体口味的指认 */
byId['n14'].text = '阿栗说，半夜她听见厨房有响动，起来在门边站了一会儿。小陶背对着她搅面糊，搅得很用力。趁他转身去拿鸡蛋，她伸手指在盆边蘸了一下，那点味道她当场就尝出来了。她没吱声，回屋躺下，装作一直睡着。蜡烛的火苗晃了晃，蛋糕还摆在桌子中间，切面敞着，那半句话这下全落到了小陶身上。';

['editorReview', 'pathReview', 'fixLog', 'fixRounds', 'regressions', 'mergeCheck', 'finalCheck', 'check'].forEach(k => { delete d[k]; });
fs.writeFileSync(p, JSON.stringify(d, null, 1), 'utf8');
console.log('终稿 v3 已应用：n9 恢复圆场、n12/n13/n14 三个结局去掉口味断言');
