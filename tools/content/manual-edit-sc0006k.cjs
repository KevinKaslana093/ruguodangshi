// 人工式定点编辑（终稿 v9）：修正 v8 引入的因果矛盾。
//
// v8 的错：让小满拿来自己的糖，却仍写"第二炉还是咸的，怪糖罐"——逻辑不成立。
// 正确写法：第二炉用了小满的糖，因此**确实救回来了（甜）**；
// 但故事此刻的焦点不是味道，而是「小陶要不要把做砸这件事说出口」
// ——所以把所有下游断言味道的措辞，改成断言"这件事要不要说破"。
const fs = require('fs');
const path = require('path');
const R = path.resolve(__dirname, '..', '..');
const p = path.join(R, 'content', 'drafts', 'sc-0006.json');
const d = JSON.parse(fs.readFileSync(p, 'utf8'));
const s = d.story;
const byId = {};
s.nodes.forEach(n => { byId[n.id] = n; });

/* n6：第二炉用糖救回来了，但小陶仍开不了口。 */
byId['n6'].text = '小满回屋翻自己的抽屉，摸出半包没拆完的糖，倒在台面上。第二炉是在天快亮时出的烤箱，蛋糕在盘子里歪着，边上一圈颜色深浅不一，中间还塌了一小块。小满拿筷子戳了一角尝，说这回行了。小陶也尝了一口，甜的，但他没说话——头一炉那件事还在台面上摊着，两盘并排摆着，一眼就能看出哪盘是坏的。他把那盘咸的往里面挪了挪。窗外的天从黑转成灰。';

/* n12（e1）：味道不再是重点，重点是"说破"这件事。 */
byId['n12'].text = '阿栗又叉了一块，含在嘴里，慢慢点了点头，说这样也挺好吃，像哪家铺子会做的点心。小满先没忍住，笑出声，伸手也切了一角。小陶的脸烫起来，叉子犹豫着跟过去。三个人围着那两盘蛋糕，一人一角地挖，蜡烛烧到一半，火苗在没开灯的屋里晃。天从灰里透出一点白，盘底慢慢刮出了声音。小陶到底没把那句话说出口。';

/* n13（e2）：同样不断言味道。 */
byId['n13'].text = '蜡烛烧到一半，蜡油在盘边积了一小滩。阿栗把手里那一角吃完，越吃越慢，最后把叉子放下了。小陶和小满也没再动。桌上那盘坏的始终摆在靠里那一侧，谁也没去碰。盘子里还剩下大半个，切面敞着。窗外那片灰终于泛出白，屋里的灯始终没开。';

['editorReview', 'pathReview', 'fixLog', 'fixRounds', 'regressions', 'mergeCheck', 'finalCheck', 'check'].forEach(k => { delete d[k]; });
fs.writeFileSync(p, JSON.stringify(d, null, 1), 'utf8');
console.log('终稿 v9 已应用：n6 第二炉救回（甜）、n12/n13 不再断言味道，改述"说破与没说破"');
