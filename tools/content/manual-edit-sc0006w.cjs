// 人工式定点编辑（定稿 v21 — 结构性定稿，消除"形状漂移"）。
//
// 诊断：n6（整盘端上）与 n7（切成小块）是**同层**两个节点，它们在 n8/n9 汇合，
//   而下游所有节点（n10 阿栗切下一角 / n13 结局）必须同时容纳两支 →
//   蛋糕形状在每条路径上都可能对不上，改词永远按不住。
//   （这与之前已经修掉的"味道漂移"是完全同类的结构错误。）
//
// 定稿：n6/n7 **都不改变蛋糕的形态**（n1 已交代"切开的角敞着"＝试味时切过一角），
//   两支的差异**只在态度**：n6 = 硬着头皮不说；n7 = 想粉饰成"新口味"。
//   下游因此恒有"切下一角"的余地。
const fs = require('fs');
const path = require('path');
const R = path.resolve(__dirname, '..', '..');
const p = path.join(R, 'content', 'drafts', 'sc-0006.json');
const d = JSON.parse(fs.readFileSync(p, 'utf8'));
const s = d.story;
const byId = {};
s.nodes.forEach(n => { byId[n.id] = n; });

/* ① n6：硬着头皮，形态不变 */
byId['n6'].text = '小陶把盘子端到桌子那头，刀叉摆在旁边，动作比平时慢。小满靠在橱柜边看着他，没帮忙，也没走开。那盘蛋糕的切角敞着，咸味还没散。小陶把卡片收进兜里，窗外的天从黑转成灰。他什么也没解释，只说了句：端上去吧。';
/* ② n7：想粉饰，形态同样不变 */
byId['n7'].text = '小陶把盘子端到桌子那头，又拿抹布把盘沿擦了擦，摆得比平时齐整。小满靠在橱柜边看着他，没帮忙，也没走开。写着糖的那只罐子被推到角落，标签还朝外翻着。小陶把卡片收进兜里，窗外的天开始从黑转灰。他说：就当是新口味。';

/* ③ n6/n7 的选项：与正文一致（不再有"切块""贴便签"） */
byId['n6'].choices.forEach(c => {
  if (c.to === 'n8') c.text = '把话憋住，只把盘子放下';
  if (c.to === 'n9') c.text = '先看看阿栗第一口的反应';
});
byId['n7'].choices.forEach(c => {
  if (c.to === 'n8') c.text = '摆好盘沿，等着被问';
  if (c.to === 'n9') c.text = '绝口不提配方，先看反应';
});

/* ④ n1：补蛋糕盒的铺垫（n3 提到"还没派上用场的盒子"） */
byId['n1'].text = '橱柜最上层那个贴着「糖」字的罐子，是上回装盐时贴错了标签，一直没人改；小陶把它拿下来搁在台面上，又翻出那只真正的糖袋——早见了底。小陶半夜起来烤蛋糕，照标签舀了两勺，直到第一炉出烤箱尝了一口才发现不对。台面上那盘蛋糕切开的角敞着（试味时切下的那一角），咸味在厨房里散不掉；旁边压着一张贺卡和一只空着的蛋糕盒，都是给阿栗准备的。走廊两头，阿栗和小满的房门都关着。窗外还是黑的，屋里只亮着一盏小灯。';

/* ⑤ n13 结局：不描述蛋糕形态与盘子的位置 */
byId['n13'].text = '蜡烛烧到一半，蜡油在盘边积了一小滩。桌上慢慢安静下来，谁也没再开口。窗外那片灰终于泛出白，屋里的灯始终没开，谁也没去开。那盘东西还剩着，谁也没去收。';

/* ⑥ n12 结局：不再断言"他没把那句话说出口"（n8 的撒谎选项需要成立） */
byId['n12'].text = '阿栗又叉了一块，含在嘴里，慢慢点了点头，说这样也挺好吃，像哪家铺子会做的点心。小满先没忍住，笑出声，伸手也切了一角。小陶的脸烫起来，叉子犹豫着跟过去。三个人围着那盘蛋糕，一人一角地挖，蜡烛烧到一半，火苗在没开灯的屋里晃。天从灰里透出一点白，盘底慢慢刮出了声音。';

/* ⑦ n6/n7 正文里的卡片状态与 n3 两种选择都兼容（"收进兜里"不描述写没写完） */
byId['n3'].text = '小陶把贺卡摊在台面上，笔帽咬在嘴里。他写下「阿栗，生日快乐」，又写了两行平常的话，笔尖停在最后一行上，没往下写。台面另一头，蛋糕和那只空盒子并排搁着。咸味在厨房里散不掉。他在想，要不要在贺卡里提一句——不提，阿栗第一口就会尝到；提了，这个生日就从一开始带着道歉。';

['editorReview', 'pathReview', 'fixLog', 'fixRounds', 'regressions', 'mergeCheck', 'finalCheck', 'check'].forEach(k => { delete d[k]; });
fs.writeFileSync(p, JSON.stringify(d, null, 1), 'utf8');
console.log('定稿 v21 已应用：n6/n7 形态统一（消除形状漂移）、选项对齐、n1 补盒、n12/n13 去形态断言');
