/* 收尾修复：sc-0009 / sc-0010 / sc-0012 各自的最后一条 major。
 *
 * sc-0009 [major]：「n6 受理台与 n8/n9 门口房间场景混同，同一人先称规矩不可破又当场破例」
 *   根因：n6（受理台）与 n7（无门牌房间）是两个不同场景，却都直接通向 n8/n9（合并点）。
 *   修法：n6 结尾由受理台把人**指向走廊尽头那间没挂牌的屋**，使 n6/n7 都在同一场景汇合；
 *        n8/n9 的人物指代统一为「桌后那人」。
 *
 * sc-0010 [major]：「走数据路线时结局说小林交的稿与组长旧稿是同一个注水套路，与表格稿矛盾」
 *   修法：n14 去掉「与哪份稿像不像」的比较，改为按**新口径**反讽：
 *        「情绪词一行接一行——按新口径，这些一个字都不算」。两条来路都成立。
 *
 * sc-0012 [major]：「n11 提到的人事文档在 n4 来路上从未铺垫」
 *   修法：n11 不再提「文档」，只问「要不要再往上问一层」；
 *        n13 的悬念改为「名单是谁改的」；n14 在揭晓处才引出文档（twist 的正当出现方式）。
 */
'use strict';
const fs = require('fs');
const path = require('path');
const R = path.resolve(__dirname, '..', '..');
const V = require(path.join(R, 'shared', 'validate.js'));

const cjk = s => (String(s).match(/[\u4e00-\u9fff]/g) || []).length;
const CLEAR = ['editorReview', 'pathReview', 'fixLog', 'fixRounds', 'regressions', 'mergeCheck', 'finalCheck', 'check', 'consistencyReview'];

function load(id) {
  const p = path.join(R, 'content', 'drafts', id + '.json');
  const d = JSON.parse(fs.readFileSync(p, 'utf8'));
  const m = {};
  d.story.nodes.forEach(n => { m[n.id] = n; });
  return { p, d, m };
}
function save(p, d) {
  CLEAR.forEach(k => { delete d[k]; });
  fs.writeFileSync(p, JSON.stringify(d, null, 1), 'utf8');
}

/* ---------- sc-0009 ---------- */
{
  const { p, d, m } = load('sc-0009');
  /* n6：把两条来路汇到同一场景（走廊尽头没挂牌的屋） */
  m.n6.text = '数据治理办的受理台前排队。老周把要凑三个废章的事讲了一遍，柜台后的人把屏幕转过来：状态确认表由系统生成，人工打不了；系统只认当初发的那张工牌。老周说，工牌在我手里。对方说，你手里那张刷出来是什么，你比我清楚。小丁把老周往旁边拉了半步。柜台后的人往走廊里努了努嘴：尽头那间没挂牌的屋，去问问吧，兴许有人肯认这个章。';
  /* n8/n9：人物指代统一（与 n7 的「桌后坐着一个人」一致） */
  m.n8.text = '老周没绕弯，把表推过去，一格一格说用途：三个无人认领的废章，凑齐了才能撤销。桌后那人接过去翻了翻，抬眼问他，前面那几处都是怎么说的。老周把一路听来的话原样复述了一遍：要表、要章、处处卡着。那人听完把表放下：那些规矩，到我这门口为止。说完弯身拉开抽屉，翻出一枚章搁在表边上，章面朝下。小丁盯着那枚章，手在裤缝上蹭了蹭。谁也没去碰它。';
  m.n9.text = '老周没提表，先打听起无人认领的章——原先归谁管，是谁留下的。桌后那人把笔搁下，看了他一眼：章的主人早不在了，走的时候东西没人接，章就撂在这儿，也没人来认。小丁在旁边小声说，那不就是没人认领。那人没接这句，只说：前面那几处的规矩，到我这儿为止——又问，你的工号是多少。表还摊在桌沿上，三个格子空着。';
  save(p, d);
  console.log('[sc-0009] n6=' + cjk(m.n6.text), 'n8=' + cjk(m.n8.text), 'n9=' + cjk(m.n9.text));
}

/* ---------- sc-0010 ---------- */
{
  const { p, d, m } = load('sc-0010');
  m.n14.text = '小林把那沓旧周报抱到周组长面前，一份一份摊开。翻到中间，夹着的一份旧稿滑了出来，署名正是周组长。他顺着往下读：开头写窗外天色，中间写流程，结尾一段感悟，情绪词一行接一行——按组里新改的口径，这些一个字都不算数。周组长低头看着那一页，杯盖捏在手里没拧上，话说到一半停了。';
  save(p, d);
  console.log('[sc-0010] n14=' + cjk(m.n14.text));
}

/* ---------- sc-0012 ---------- */
{
  const { p, d, m } = load('sc-0012');
  m.n11.text = '走廊尽头，阿哲把电脑架在窗台上，开了一个空白页。小满一句句口述，莉姐替她挑字眼，把「误写」改成「表头有误」。老张守住一句：稿子得等总监签字。写到一半，小满转头问莉姐，这事要不要再往上问一层。莉姐把手机翻过来又扣下，只说先按流程。稿子停在屏幕上，一个字没发出去，入口那边数字还在跳。';
  m.n13.text = '澄清稿写定，总监从头读到尾，签了名：说明照发，报名表不撤，留着。阿哲回后台挂上那行公告——表头有误，名单原样，已经填过的名字一个没删。小满盯着屏幕上还在往上跳的报名数字，走廊里没人接话。莉姐把手机扣回掌心，老张把笔帽按上。那栏名单究竟是谁改的，四个人到底没问出来。';
  m.n14.text = '澄清稿压着没发。小满没按流程等下去，自己顺着这条线往上追，一路问到人事：那边确实存着一份文档，标题写着人员优化分组，每组前面标着优先级，编号和链接里那栏一行不差；团建那份行程分组，也从同一个文件夹里翻了出来。两份东西并排摊在窗台上，一份团建，一份预案，都还在那儿放着。小满看着那两屏，没开口。';
  save(p, d);
  console.log('[sc-0012] n11=' + cjk(m.n11.text), 'n13=' + cjk(m.n13.text), 'n14=' + cjk(m.n14.text));
}

/* ---------- 结构校验 ---------- */
let bad = 0;
['sc-0009', 'sc-0010', 'sc-0012'].forEach(id => {
  const d = JSON.parse(fs.readFileSync(path.join(R, 'content', 'drafts', id + '.json'), 'utf8'));
  const r = V.validateStory(d.story, { wantPaths: true });
  if (!r.ok) { bad++; console.log('✗ ' + id + ': ' + JSON.stringify(r.errors.slice(0, 4))); }
  else console.log('✓ ' + id + ' 结构通过');
});
process.exit(bad ? 1 : 0);
