// 人工式定点编辑（定稿 v19）。
// ① major n4：n2 选「敲门问小满有没有糖」后，n4 却写小满"自己出现"且没提糖 → 选择无后果。
//    修法：n4 开头写"敲门后他出来"，并把糖的事交代掉（他去翻了抽屉，只找到一点点）。
// ② major n14：n11 已坦白，n14 却说"那半句话还没落地" → 冲突。
//    修法：n14 只写阿栗的回忆与此刻的天光，不引用"半句话"。
// ③ minor n5：糖罐位置口径（n1 在橱柜最上层 / n5 在台面上）→ n1 改"后来被拿到台面上"。
const fs = require('fs');
const path = require('path');
const R = path.resolve(__dirname, '..', '..');
const p = path.join(R, 'content', 'drafts', 'sc-0006.json');
const d = JSON.parse(fs.readFileSync(p, 'utf8'));
const s = d.story;
const byId = {};
s.nodes.forEach(n => { byId[n.id] = n; });

/* ① n4 */
byId['n4'].text = '小陶敲了两下，小满隔着门应了一声，趿着拖鞋出来了，头发压得乱七八糟。小陶把切下的那一角推到他面前，说了实话：罐子拿错了，舀进面糊里的是盐。小满捏起蛋糕咬了一口，嚼了两下，没咽下去，也没吐出来，只把杯子往边上挪了挪。他说糖的事他回头翻翻自己抽屉，可这会儿天快亮了，重做来不及。台面上那盘蛋糕切开的角敞着，咸味还没散。两个人站在小灯底下，怎么办还没商量出来。';

/* ② n14 结局 */
byId['n14'].text = '阿栗说，半夜她听见厨房有响动，起来在门边站了一会儿。小陶背对着她搅面糊，搅得很用力。趁他转身去拿鸡蛋，她伸手指在盆边蘸了一下，那点味道她当场就尝出来了。她没吱声，回屋躺下，装作一直睡着。蜡烛的火苗晃了晃，蛋糕还摆在桌子中间，切面敞着。她早就等着看，他到底会不会自己说。';

/* ③ n1：糖罐位置口径 */
byId['n1'].text = '橱柜最上层那个贴着「糖」字的罐子，是上回装盐时贴错了标签，一直没人改；小陶把它拿下来搁在台面上，又翻出那只真正的糖袋——早见了底。小陶半夜起来烤蛋糕，照标签舀了两勺，直到第一炉出烤箱尝了一口才发现不对。台面上那盘蛋糕切开的角敞着，咸味在厨房里散不掉；旁边压着一张没写完的贺卡，是给阿栗的。走廊两头，阿栗和小满的房门都关着。窗外还是黑的，屋里只亮着一盏小灯。';

['editorReview', 'pathReview', 'fixLog', 'fixRounds', 'regressions', 'mergeCheck', 'finalCheck', 'check'].forEach(k => { delete d[k]; });
fs.writeFileSync(p, JSON.stringify(d, null, 1), 'utf8');
console.log('定稿 v19 已应用：n4 兑现敲门与糖、n14 去"半句话"、n1 统一糖罐位置');
