// 人工式定点编辑（定稿 v20）。
// ① major n3：贺卡写了道歉，后续却被扣住 → 选择无后果。
//    修法：让"卡片写了什么"在 n8（桌上）以不泄露具体内容的方式落地：
//    贺卡压在盘子边、朝上（写了就朝上，没写就扣着由玩家自己判断——改为统一朝上，
//    并在 n8 写"卡片摆在盘子边上"，不再说扣着；扣着的说法只在未写的那条线才成立，
//    因此这里改为**不描述朝上朝下**，只说"贺卡压在盘子边上"）。
// ② minor n7：选项说"装盒贴便签"，正文是切块摆盘 → 选项改为与正文一致。
// ③ minor n5：n2 说小满房门关着，n5 他无声出现 → n5 开头补开门声。
// ④ major 路径1：n10 选「把盘子推远」，n13 却说"盘子摆在桌子中间" → n13 不描述盘子位置。
const fs = require('fs');
const path = require('path');
const R = path.resolve(__dirname, '..', '..');
const p = path.join(R, 'content', 'drafts', 'sc-0006.json');
const d = JSON.parse(fs.readFileSync(p, 'utf8'));
const s = d.story;
const byId = {};
s.nodes.forEach(n => { byId[n.id] = n; });

/* ① n6/n8：卡片不再描述正反面；n6 只写他把卡片收进兜里（两条来路都成立） */
byId['n6'].text = '小陶把蛋糕端到台面中间，刀、叉、盘子一件件摆开，动作比平时慢。小满靠在橱柜边看着他，没帮忙，也没走开。台面上那盘蛋糕颜色发暗，切开的角敞着，咸味还没散。小陶把那张写到一半的卡片收进兜里。窗外的天从黑转成灰。他深吸一口气，说了句：端上去吧。';
byId['n7'].text = '小陶把那盘蛋糕端到台面中间，拿刀切成一块一块，切面朝上码着，谁想吃哪块自己拿。小满靠在橱柜边看着他，没帮忙，也没走开。写着糖的那只罐子被推到角落，标签还朝外翻着。小陶把那张写到一半的卡片收进兜里。窗外的天开始从黑转灰。他说：就当是新口味。';
byId['n8'].text = '窗外刚泛出一点灰，阿栗被叫起来，坐在桌边，头发还压着一道印。蜡烛插上了，火苗在没开灯的屋里晃。蛋糕端到了桌子中间，阿栗伸手去拿刀叉，嘴里道了声谢。小陶站在她手边，盯着桌上那东西，喉咙里那句话已经排到嘴边，还没出声。蜡油顺着蜡烛往下淌。';

/* ② n7 选项：与正文（切块摆盘）一致 */
byId['n7'].choices.forEach(c => {
  if (c.to === 'n8') c.text = '码好摆盘，不提这是怎么回事';
  if (c.to === 'n9') c.text = '切好摆盘，绝口不提配方';
});

/* ③ n5：补开门铺垫 */
byId['n5'].text = '隔壁房门响了一下，小满趿着拖鞋站在了厨房门口。小陶把身子挡在蛋糕前，说那是新试的口味，让他回去睡。小满没答话，绕过来，用指甲从切开的角上抠下一块放进嘴里。他嚼得很慢，眉头动了一下，然后看向台面上那只写着糖的罐子，又看向小陶。咸味在厨房里散不掉。他把罐子端起来，掂了掂。';

/* ④ n13：不描述盘子的位置 */
byId['n13'].text = '蜡烛烧到一半，蜡油在盘边积了一小滩。桌上慢慢安静下来，谁也没再开口。窗外那片灰终于泛出白，屋里的灯始终没开，谁也没去开。切面朝上码着的那些小块，还剩着大半。';

['editorReview', 'pathReview', 'fixLog', 'fixRounds', 'regressions', 'mergeCheck', 'finalCheck', 'check'].forEach(k => { delete d[k]; });
fs.writeFileSync(p, JSON.stringify(d, null, 1), 'utf8');
console.log('定稿 v20 已应用：卡片统一处理、n7 选项对齐、n5 补开门、n13 去盘子位置');
