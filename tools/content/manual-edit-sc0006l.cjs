// 人工式定点编辑（终稿 v10 — 定稿）。
//
// 反复出现的根因已清晰：**合并点与结局携带了只在部分来路成立的道具**——
// 「两盘蛋糕」（只有重烤线才有第二炉）、「贺卡」（只有 n3 线才写）、
// 「糖罐的位置」（只有 n5 线被端起来）。
//
// 定稿原则：合并点与结局只写**所有来路都成立的东西**：蛋糕 + 蜡烛 + 天光 + 人的动作。
// 一件道具如果只在某条线上存在，就不能出现在汇合之后的任何节点里。
const fs = require('fs');
const path = require('path');
const R = path.resolve(__dirname, '..', '..');
const p = path.join(R, 'content', 'drafts', 'sc-0006.json');
const d = JSON.parse(fs.readFileSync(p, 'utf8'));
const s = d.story;
const byId = {};
s.nodes.forEach(n => { byId[n.id] = n; });

/* ① n2：贺卡在 n1 未出现 → 在 n1 交代台面上有贺卡（n1 是所有路径的起点，安全）。 */
byId['n1'].text = '橱柜最上层那罐贴着「糖」字的罐子，其实是上回装盐时贴错了标签，一直没人改。小陶半夜起来烤蛋糕，舀了两勺进去，直到第一炉出烤箱尝了一口才发现不对。台面上那盘蛋糕切开的角敞着，咸味在厨房里散不掉；旁边压着一张没写完的贺卡，是给阿栗的。窗外还是黑的，屋里只亮着一盏小灯。';
/* n2 不再单独提"那张贺卡"（n1 已交代，措辞对齐即可） */
byId['n2'].text = '小陶把橱柜翻了个底朝天。面粉袋还有半袋，鸡蛋也够，糖袋却只剩一个空壳，抖了抖，连底子里的碎粒都没倒出来。他蹲在柜门前，把空糖袋捏扁又展开。再烤一炉的话，缺的就是这一样。走廊那头，小满的房门关着，屋里没灯。台面上，咸蛋糕和那张写到一半的贺卡一起搁着，切开的角敞着。';

/* ② n12 结局：去掉「两盘」——只有重烤线才有第二炉。 */
byId['n12'].text = '阿栗又叉了一块，含在嘴里，慢慢点了点头，说这样也挺好吃，像哪家铺子会做的点心。小满先没忍住，笑出声，伸手也切了一角。小陶的脸烫起来，叉子犹豫着跟过去。三个人围着桌上那盘蛋糕，一人一角地挖，蜡烛烧到一半，火苗在没开灯的屋里晃。天从灰里透出一点白，盘底慢慢刮出了声音。小陶到底没把那句话说出口。';

/* ③ n13 结局：去掉「那盘坏的」「剩下大半个」等只在部分来路成立的说法。 */
byId['n13'].text = '蜡烛烧到一半，蜡油在盘边积了一小滩。阿栗把手里那一角吃完，越吃越慢，最后把叉子放下了。小陶和小满也没再动。桌上那点事始终没被说破，谁也没去碰那盘子。窗外那片灰终于泛出白，屋里的灯始终没开。';

/* ④ n7：不再断言糖罐的位置（n5 线它被端起来过）。 */
byId['n7'].text = '小陶把烤好的那盘端出来搁在台面上，切开的角敞着。他和小满站在台面两头，谁也没先坐下。写着糖的那只罐子被推到角落，标签还朝外翻着。窗外的天开始从黑转灰。小满说重做来不及了，先想怎么办。';

/* ⑤ n14 结局：同样不指定盘数/味道。 */
byId['n14'].text = '阿栗说，半夜她听见厨房有响动，起来在门边站了一会儿。小陶背对着她搅面糊，搅得很用力。趁他转身去拿鸡蛋，她伸手指在盆边蘸了一下，那点味道她当场就尝出来了。她没吱声，回屋躺下，装作一直睡着。蜡烛的火苗晃了晃，蛋糕还摆在桌子中间，切面敞着，那半句话这下全落到了小陶身上。';

['editorReview', 'pathReview', 'fixLog', 'fixRounds', 'regressions', 'mergeCheck', 'finalCheck', 'check'].forEach(k => { delete d[k]; });
fs.writeFileSync(p, JSON.stringify(d, null, 1), 'utf8');
console.log('终稿 v10 已应用：n1 交代贺卡、n2 对齐、n12/n13/n14 去盘数与味道断言、n7 去糖罐位置');
